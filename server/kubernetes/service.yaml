apiVersion: v1
kind: Service
metadata:
  name: open-rl-server
spec:
  type: ClusterIP
  selector:
    app: open-rl-server
  ports:
  - name: gateway
    protocol: TCP
    port: 80
    targetPort: 8000
