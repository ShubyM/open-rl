# The smoke test has no model to run; placement neither knows nor cares.
apiVersion: v1
kind: Pod
spec:
  restartPolicy: Never
  containers:
    - name: worker
      image: busybox:1.36
      command: ["sleep", "infinity"]
