apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-rl-shared-pvc
  labels:
    app: open-rl
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: filestore-custom
  resources:
    requests:
      storage: 1200Gi
